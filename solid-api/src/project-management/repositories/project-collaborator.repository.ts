import { Injectable } from '@nestjs/common';
import { SecurityRuleRepository } from '@solidxai/core';
import { SolidBaseRepository } from '@solidxai/core' ;
import { RequestContextService } from '@solidxai/core';
import { DataSource } from 'typeorm';
import { InjectDataSource } from '@nestjs/typeorm';
import { ProjectCollaborator } from '../entities/project-collaborator.entity';

@Injectable()
export class ProjectCollaboratorRepository extends SolidBaseRepository<ProjectCollaborator> {
    constructor(
        @InjectDataSource("default")
        readonly dataSource: DataSource,
        readonly requestContextService: RequestContextService,
        readonly securityRuleRepository: SecurityRuleRepository,
    ) {
        super(ProjectCollaborator, dataSource, requestContextService, securityRuleRepository);
    }
}